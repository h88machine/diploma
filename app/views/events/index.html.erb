<h1>Event List</h1>
<div class="row">
	<div class="span6 offset3">
		<% @event.each do |event|%>
		<li>
			<%= link_to event.name, event%>
		</li>
		<% end %>
		
		<%= calendar @event do |event| %>
		
		<div>
			<%= link_to event.name, event %>
		</div>
		<% end %>
		
	</div>
</div>