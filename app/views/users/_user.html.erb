<li>
	<%= gravatar_for user, size: 52 %>
	<%= link_to user_list.name, user %>
	<% if current_user.admin? && !current_user?(user) %>
		| <%= link_to "delete", user, method: :delete, confirm: "R U shure?" %>
		
	<% end %>
</li>
